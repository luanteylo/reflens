# Set this to override the API URL. When unset, the frontend auto-detects
# the hostname from the browser (client-side) or os.hostname() (server-side).
# NEXT_PUBLIC_API_URL=http://localhost:8000/api/v1

# Auth: set to "true" to enable login/signup redirect
NEXT_PUBLIC_AUTH_ENABLED=True
